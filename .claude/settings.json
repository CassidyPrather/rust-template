{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Edit|Write",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.rs|*Cargo.toml) cargo fmt --all && cargo run --quiet --bin gen-docs;; esac; } 2>/dev/null || true",
                        "timeout": 120,
                        "statusMessage": "cargo fmt + gen-docs"
                    }
                ]
            }
        ]
    }
}
